# codex-language

Codex `UserPromptSubmit` hook component that detects the developer's input language and asks Codex to use that language for conversational output. It preserves code, identifiers, file paths, and command syntax.

The component runs locally with Node, reads only the hook payload and optional transcript, and never makes network calls.
